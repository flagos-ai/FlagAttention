# Copyright 2026 FlagOS Contributors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

[project]
name = "flag_attn"
dynamic = ["version"]
authors = [
    {name = "Chen Feiyu", email = "iclementine@outlook.com"},
]
description = "A collection of memory efficient attention operators implemented in triton language."
readme = {file= "README.md", content-type="text/markdown"}
requires-python = ">=3.10"
license = {text = "LICENSE.txt"}
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
]

dependencies = []

[project.optional-dependencies]
triton = [
    "triton>=2.2.0",
]
test = [
    "pytest>=7.1.0",
    "PyYAML>=6.0",
]

[project.urls]
homepage = "https://github.com/FlagOpen/FlagAttention"


[build-system]
requires = ["setuptools>=60", "setuptools-scm>=8.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_file = "src/flag_attn/_version.py"

[tool.setuptools.packages.find]
where = ["src"]
include = ["flag_attn", "flag_attn.*"]
namespaces = false

# helps for setting up pytest in pyprojects
# https://docs.pytest.org/en/7.3.x/reference/customize.html#rootdir
# https://docs.pytest.org/en/7.3.x/reference/reference.html#confval-pythonpath
[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
testpaths = [
    "tests",
]
pythonpath = [
    ".",
    "src",
    "tests/flag_attn",
]
markers = [
    "chunk_gated_delta_rule: chunked gated delta rule tests and benchmarks",
    "chunk_gdn2: chunked GDN2 tests and benchmarks",
    "chunk_gla: chunked gated linear attention tests and benchmarks",
    "chunk_gla_chunk: chunked gated linear attention",
    "chunk_gla_state_v_first: gated linear attention state layout",
    "chunk_gla_varlen: variable-length gated linear attention",
    "chunk_kda: Kimi Delta Attention",
    "minimax_m3_sparse_attn: MiniMax M3 sparse attention tests and benchmarks",
    "minimax_sparse_attention_decode: MiniMax sparse attention decoding",
    "minimax_sparse_attention_prefill: MiniMax sparse attention prefill",
    "minimax_sparse_attention_topk: MiniMax sparse attention top-k selection",
    "parallel_nsa: native sparse attention",
    "parallel_nsa_compression: native sparse attention compression",
    "sage_attention: SageAttention tests and benchmarks",
]

[tool.ruff]
ignore = ["E741"]
line-length = 120
